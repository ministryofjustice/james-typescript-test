name: Security trivy dependency check MAIN

on:
  workflow_dispatch:
  schedule:
    - cron: "30 5 * * MON-FRI" # Every weekday at 05:35 UTC

jobs:
  security-kotlin-trivy-check:
    name: Project security trivy dependency check
    uses: ./.github/workflows/security_trivy_TEST.yml
#    uses: ministryofjustice/hmpps-github-actions/.github/workflows/security_trivy.yml@0.1
    with:
      channel_id: ${{ vars.SLACK_CHANNEL_ID }}
    secrets:
      SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
