name: Security npm dependency check
on:
  workflow_dispatch:
  schedule:
    - cron: "${{ vars.DAILY_CRON }}" # Every weekday at 05:35 UTC

jobs:
  security-npm-dependency-check:
    name: Project security npm dependency check
#    uses: ministryofjustice/hmpps-github-actions/.github/workflows/security_npm_dependency.yml@v0.3 #WORFKLOW VERSION BUMP
    uses: ministryofjustice/hmpps-github-actions/.github/workflows/security_npm_dependency.yml@v1.0 #WORFKLOW VERSION
#    uses: ./.github/workflows/security_npm_dependency_TEST.yml
    with:
      channel_id: ${{ vars.SECURITY_ALERTS_SLACK_CHANNEL_ID || 'NO_SLACK' }}
    secrets: inherit

      